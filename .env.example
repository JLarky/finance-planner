# Production: set a strong secret in Deno Deploy.
SESSION_SECRET=replace-me
# Optional local KV endpoint. On Deno Deploy, Deno.openKv() uses the linked DB.
# DENO_KV_URL=https://api.deno.com/v2/databases/<id>/connect
# WEBAUTHN_RP_NAME=Finance Planner
